---
wave: 1
depends_on: []
files_modified:
  - src/hipai/models.py
  - src/hipai/synthesis.py
  - src/hipai/world_model.py
autonomous: true
---

# Phase 3, Wave 1: Propositional Attitudes and Model Updates

## Objective
Enable the system to represent and parse attitude verbs (Believe, Know, Say) by linking entities to nested observations in the graph.

## Tasks

<task>
<read_first>
- `src/hipai/models.py`
</read_first>
<action>
1. Update `Relation` and `Observation` models in `src/hipai/models.py`.
   - Add `modality: Optional[Literal["must", "can", "may", "should"]] = None`.
   - Add `is_factive: bool = False` to `Relation`.
2. Update `Observation` to include `subject_id: Optional[str] = None`.
</action>
<acceptance_criteria>
- Pydantic models validate with the new optional fields.
</acceptance_criteria>
</task>

<task>
<read_first>
- `src/hipai/synthesis.py`
</read_first>
<action>
1. In `src/hipai/synthesis.py` (`add_belief`), implement Pattern 11: Attitude Verbs.
   - Use regex: `r"^(.+?)\s+(believes?|knows?|says?|thinks?)\s+that\s+(.+)$"`
   - Recursively call `self.add_belief(proposition)` to get the nested `Observation`.
   - Create a new `Observation` representing the attitude itself.
   - Return a new parse type: `attitude_belief`.
2. Update the incorporation logic to handle `attitude_belief`:
   - Link the Subject Entity to the nested Observation node using an edge labeled with the attitude verb (uppercase).
</action>
<acceptance_criteria>
- "Alice believes that Socrates is a man" parses correctly.
- The graph shows (Alice)-[:BELIEVES]->(Obs_Socrates_Man).
</acceptance_criteria>
</task>

<task>
<read_first>
- `src/hipai/world_model.py`
</read_first>
<action>
1. Update `WorldModel.incorporate_observation` to handle the new `modality` and `subject_id` fields when creating nodes and edges.
</action>
<acceptance_criteria>
- Observation nodes in FalkorDB store `modality` and `subject_id` properties.
</acceptance_criteria>
</task>

## Verification
<must_haves>
- [ ] Test "Alice believes that Socrates is a man" results in an edge from Alice to the observation node.
- [ ] Test "Bob knows that humans are mortal" results in a factive knowledge link.
</must_haves>
