---
phase: 09
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified: [docs/logic/ROOT, docs/logic/Paraclete_Foundation.thy, Makefile]
autonomous: true
requirements: [isabelle-theory, isabelle-build, isabelle-automation]
---

# Plan 09-02: Paraclete Verification Landing

<objective>
Configure the Isabelle session, finalize the Paraclete_Foundation theory, and automate the formal verification build.
</objective>

<tasks>

<task type="execute">
<files>
- docs/logic/ROOT
</files>
<read_first>
- docs/logic/ISABELLE_INTEGRATION.md
</read_first>
<action>
Create an Isabelle `ROOT` file in `docs/logic/` to define the formal verification session.
Content:
```isabelle
session HiPAI_Foundation = HOL +
  theories
    Paraclete_Foundation
```
</action>
<verify>
Check for file existence and content.
</verify>
<acceptance_criteria>
- `docs/logic/ROOT` exists.
- `docs/logic/ROOT` contains `session HiPAI_Foundation = HOL`.
</acceptance_criteria>
</task>

<task type="execute">
<files>
- docs/logic/Paraclete_Foundation.thy
</files>
<read_first>
- docs/logic/Paraclete_Foundation.thy
- src/hipai/ontology_manager.py
</read_first>
<action>
Verify that `docs/logic/Paraclete_Foundation.thy` matches the current seed axioms in `src/hipai/ontology_manager.py`.
Specifically, ensure the concepts `CAction`, `CAgent`, `CPatient`, and `CEntity` match the classes seeded in `OntologyManager.seed_axioms()`.
</action>
<verify>
Read both files and compare class/concept lists.
</verify>
<acceptance_criteria>
- `Paraclete_Foundation.thy` concepts match `ontology_manager.py` seed classes.
</acceptance_criteria>
</task>

<task type="execute">
<files>
- Makefile
</files>
<read_first>
- Makefile
</read_first>
<action>
Add a `verify-logic` target to the project `Makefile` to automate the Isabelle build.
Command: `isabelle build -D docs/logic`
(Note: Use the full path `/home/ty/Isabelle2025-2/bin/isabelle` if not yet in interactive shell PATH).
</action>
<verify>
Run `make verify-logic`.
</verify>
<acceptance_criteria>
- `Makefile` contains `verify-logic` target.
- `make verify-logic` executes the isabelle build.
</acceptance_criteria>
</task>

<task type="execute">
<files>
- docs/logic/Paraclete_Foundation.thy
</files>
<read_first>
- docs/logic/Paraclete_Foundation.thy
</read_first>
<action>
Execute the formal verification build and ensure all proofs pass.
Command: `/home/ty/Isabelle2025-2/bin/isabelle build -v -D docs/logic`
</action>
<verify>
Confirm build output.
</verify>
<acceptance_criteria>
- Build output contains "Finished HiPAI_Foundation".
- Exit code is 0.
</acceptance_criteria>
</task>

</tasks>

<verification>
1. Run `make verify-logic`.
2. Confirm that Isabelle successfully verifies `Paraclete_Foundation.thy`.
</verification>

<success_criteria>
- Isabelle session `HiPAI_Foundation` is defined.
- `Paraclete_Foundation.thy` is machine-checked and proofs pass.
- Logic verification is integrated into the build system (Makefile).
</success_criteria>
